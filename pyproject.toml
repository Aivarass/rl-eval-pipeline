[project]
name = "rl-eval-pipeline"
version = "0.1.0"
dependencies = ["openai", "scipy"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["eval_pipeline*"]